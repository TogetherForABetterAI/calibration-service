syntax = "proto3";

package calibration;

message PredictionList {
  repeated float values = 1;
  int32 label = 2;
}

message Predictions {
  repeated PredictionList pred = 1;
  bool eof = 2;
  int32 batch_index = 3;
}

